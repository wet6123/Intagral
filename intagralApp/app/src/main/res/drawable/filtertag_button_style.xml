<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버튼이 눌렸을 때 -->
    <item android:state_selected="true" android:drawable="@drawable/layerlist_filtertag_checked">
    </item>

    <!-- 버튼이 안 눌렸을 때(디폴트) -->
    <item android:state_selected="false" android:drawable="@drawable/layerlist_filtertag_unchecked">
    </item>

</selector>